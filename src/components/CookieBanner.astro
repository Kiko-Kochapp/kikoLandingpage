<div
        id="cookie-banner"
        class="fixed bottom-6 right-6 max-w-sm bg-white border-4 border-cup-green p-6 rounded-[2rem] shadow-2xl z-[100] transform translate-y-20 opacity-0 transition-all duration-500 pointer-events-none"
>
    <div class="flex items-start gap-4">
        <span class="text-4xl">🛡️</span>
        <div>
            <h3 class="font-black text-lg mb-1 text-kid-text">Privatsphäre? Ehrensache!</h3>
            <p class="text-xs text-gray-600 mb-4 leading-relaxed">
                Wir nutzen <strong>keine Analytics</strong> und tracken dich nicht. Wir speichern lediglich deine Newsletter-Anmeldung in unserer Datenbank, wenn du dich einträgst.
            </p>

            <button
                    id="close-banner"
                    class="px-5 py-2 bg-cup-green text-white text-sm font-black rounded-xl hover:scale-105 transition shadow-sm"
            >
                Alles klar!
            </button>
        </div>
    </div>
</div>

<script>
    const banner = document.getElementById('cookie-banner');
    const closeBtn = document.getElementById('close-banner');

    if (!localStorage.getItem('privacy-info-seen')) {
        setTimeout(() => {
            banner?.classList.remove('translate-y-20', 'opacity-0', 'pointer-events-none');
        }, 1500);
    }

    closeBtn?.addEventListener('click', () => {
        localStorage.setItem('privacy-info-seen', 'true');
        banner?.classList.add('translate-y-20', 'opacity-0', 'pointer-events-none');
    });
</script>